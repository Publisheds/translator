// Static API keys — replace with your own values before building.
// In a future version these will come from a backend login service.
export const API_KEYS = {
  openai:   "sk-REPLACE_WITH_OPENAI_KEY",
  deepseek: "sk-REPLACE_WITH_DEEPSEEK_KEY",
  deepl:    "REPLACE_WITH_DEEPL_FREE_KEY:fx",
}
